# 常见问题解答 (QA)

## 硬件连接问题

### 风扇转速显示问题

**问题描述**：

- MQTT 中 `device/esp32c3_7df23340/state/fan_rpm` 显示 810 RPM
- 但 Home Assistant 界面中只显示"风扇转速"配置选择器（30%）和"风扇转速设置"诊断实体（30%）
- 缺少实际的风扇转速传感器显示

**根本原因**：

1. **错误的 device_class**：风扇转速传感器配置中使用了 `device_class: "frequency"`，这是不正确的
2. **异常处理不完整**：当风扇读取失败时，没有正确设置 "N/A" 值
3. **数据格式问题**：传感器数据需要确保以字符串格式发送

**解决方案**：

1. 移除风扇转速传感器的 `device_class: "frequency"` 配置
2. 完善异常处理，确保读取失败时显示 "N/A"
3. 在 HA 助手中显式转换传感器数据为字符串格式

**修复文件**：

- `app/ha.py`：修复风扇转速传感器配置
- `app/main.py`：完善异常处理和数据发布逻辑

**重要提醒 - 共地连接**：

- **必须连接共地线**：风扇的 GND 必须与 ESP32-C3 的 GND 连接
- **共地的重要性**：没有共地连接会导致转速读数异常或无法读取
- **接线检查**：确保 PWM 控制线、TACH 信号线和 GND 都正确连接

**测试步骤**：

1. 重新构建并上传代码到设备
2. 在 HA 中重新发现设备或等待自动发现
3. 检查设备页面是否显示"风扇转速"传感器实体
4. 验证传感器显示的是实际 RPM 值而不是百分比

## 功能验证

### 风扇转速控制功能

**当前实现**：

- 支持通过 HA 界面控制风扇转速：0%、30%、60%、90%
- MQTT 命令：`cmnd/{device_id}/config/select/fan_speed`
- 实时状态同步，控制后立即更新 HA 中的状态

**验证方法**：

1. 在 HA 设备页面找到"风扇转速"配置选择器
2. 尝试切换不同的转速档位
3. 观察风扇实际转速变化
4. 检查 MQTT 消息是否正确发送和接收

**常见问题**：

- **MQTT 消息解析问题**：select 命令发送的是字符串（如 "90%"），但代码尝试解析为 JSON
- **解决方案**：修改消息解析逻辑，支持字符串格式的 select 命令
- **调试方法**：查看设备日志中的风扇控制相关消息

**硬件控制问题**：

- **硬件定时器问题**：原代码使用硬件定时器可能导致初始化失败
- **解决方案**：移除硬件定时器，使用简单的计数方法，参考测试脚本逻辑
- **PWM 初始化**：简化 PWM 初始化过程，确保硬件正确初始化
- **RPM 计算**：使用周期法计算 RPM，与测试脚本保持一致

**硬件定时器资源管理**：

- **ESP32C3 限制**：只有 2 个硬件定时器（编号 0-1）
- **当前使用**：daemon.py 和 led.py 各占用 1 个硬件定时器
- **风扇模块**：不使用硬件定时器，避免资源冲突
- **配置修复**：修正硬件定时器管理器配置为 2 个定时器

**风扇转速状态显示问题**：

- **问题**：设置 90%转速后，HA 显示自动回到 30%
- **原因**：主程序中默认值逻辑错误，非标准转速值被强制设为 30%
- **解决方案**：修复状态转换逻辑，保持实际转速值显示
- **日志级别**：将日志级别设置为 DEBUG，显示所有错误和警告

**风扇控制逻辑深度修复**：

- **系统启动问题**：系统启动时强制设置默认转速 30%，覆盖用户设置
- **数据传输问题**：使用百分比字符串（如"90%"）传输，应使用纯数字
- **定期维护问题**：定期维护中重复发布状态，可能覆盖用户设置
- **风扇最低转速问题**：风扇最低转速为 10%，不能为 0%
- **解决方案**：
  - 恢复系统启动时的默认转速设置（30%）
  - 修改 HA 配置为纯 Gauge 卡片，使用 Home Assistant 原生 Gauge 卡片实现圆形进度环显示
  - 优化定期维护逻辑，仅在状态变化时发布
  - 修正风扇转速选项，将 0%改为 10%
  - 添加低速起转辅助：从 0%启动且目标转速 ≤10%时先设置 50%启动，再设置目标转速
  - 修复起转辅助逻辑：避免系统启动时 30%转速触发起转辅助

### 重启按钮确认弹窗

**当前状态**：重启按钮已添加确认弹窗，防止误操作

**实现方案**：

- 使用 HA 的 `confirmation` 配置选项
- 在按钮配置中添加确认提示："确定要重启设备吗？此操作将断开所有连接。"

**验证方法**：

1. 在 HA 设备页面找到"重启设备"按钮
2. 点击按钮，应该会弹出确认对话框
3. 确认后设备才会执行重启操作

## 配置管理

### 风扇配置

```python
"fan": {
    "pwm_pin": 2,           # PWM控制引脚
    "tach_pin": 6,          # TACH信号引脚
    "pwm_freq": 25000,      # PWM频率
    "pulses_per_rev": 2,    # 每转脉冲数
    "default_speed": 30,    # 默认转速百分比
    "enabled": True,        # 是否启用风扇功能
}
```

### HA 配置

```python
"ha": {
    "discovery_prefix": "homeassistant",
    "device_name": "Zusheng's ESP32C3",
    "manufacturer": "Zusheng STU",
    "model": "C3",
    "sw_version": "2.3.0",
    "fan_name": "风扇",      # 风扇显示名称
}
```

## 故障排除

### 风扇不工作

1. **检查硬件连接**：

   - PWM 控制线连接到 GPIO 2
   - TACH 信号线连接到 GPIO 6
   - **重要**：GND 必须连接（共地）

2. **检查配置**：

   - 确认 `fan.enabled` 为 `true`
   - 检查引脚配置是否正确

3. **检查日志**：
   - 查看设备日志中的风扇初始化信息
   - 确认是否有风扇相关的错误信息

### 转速读数异常

1. **共地连接**：确保风扇 GND 与 ESP32-C3 GND 连接
2. **TACH 信号**：检查 TACH 信号线连接
3. **脉冲数配置**：确认 `pulses_per_rev` 配置正确（利民 TL-S12W 为 2）

### HA 中不显示传感器

1. **重新发现**：在 HA 中重新发现设备
2. **检查 MQTT**：确认 MQTT 消息正确发送
3. **检查配置**：确认 HA 发现配置正确发布

## 自动化配置方案

### 方案 1：使用自动化脚本（推荐）

1. **运行配置脚本**：

   ```bash
   python setup_ha_dashboard.py
   ```

2. **复制配置文件**：

   - 将生成的 `ui-lovelace.yaml` 复制到 Home Assistant 配置目录
   - 在 `configuration.yaml` 中添加：
     ```yaml
     lovelace:
       mode: yaml
     ```

3. **重启 Home Assistant**：
   - 重启后自动显示"设备控制"视图
   - 包含风扇转速 Gauge 卡片

### 方案 2：使用 REST API 自动配置

1. **修改配置参数**：

   - 编辑 `auto_configure_dashboard.py` 中的 `HA_URL` 和 `HA_TOKEN`
   - 运行脚本：`python auto_configure_dashboard.py`

2. **自动配置**：
   - 脚本会自动连接到 Home Assistant
   - 自动创建 Gauge 卡片到仪表板

### 方案 3：手动配置（备选）

如果自动化脚本不可用，可以手动配置：

1. **进入仪表板编辑模式**
2. **添加卡片** → **手动**
3. **使用 Gauge 配置**：
   ```yaml
   type: gauge
   entity: sensor.esp32c3_7df23340_fan_speed
   name: 风扇转速
   unit: '%'
   min: 0
   max: 100
   severity:
     green: 0
     yellow: 30
     red: 70
   needle: true
   ```

## Home Assistant 实体标识符问题

### 实体ID不一致问题

**问题描述**：

在 Home Assistant 中，实体标识符的生成规则不一致，导致配置文件中的实体ID无法正确匹配：

- **风扇控制**: `climate.zusheng_s_esp32c3_feng_shan_kong_zhi`
- **温度传感器**: `sensor.esp32c3_3340_temperature`
- **湿度传感器**: `sensor.esp32c3_3340_humidity`
- **LED状态**: `sensor.zusheng_s_esp32c3_ledzhuang_tai`
- **重启按钮**: `button.zusheng_s_esp32c3_zhong_qi_she_bei`

**根本原因**：

1. **设备ID生成**: 硬件ID `esp32c3_3340` (设备唯一ID的后8位十六进制)
2. **设备名称转换**: `"Zusheng's ESP32C3"` → `zusheng_s_esp32c3`
3. **实体名称转换**: 中文转拼音，如 `"风扇控制"` → `feng_shan_kong_zhi`

**实体ID分布规律**：

- **温度/湿度传感器**: 使用硬件ID `esp32c3_3340` (配置名称是英文)
- **其他所有实体**: 使用设备名称 `zusheng_s_esp32c3` (配置名称包含中文)

**解决方案**：

使用正确的实体标识符更新配置文件：

```yaml
# 风扇控制
entity: climate.zusheng_s_esp32c3_feng_shan_kong_zhi

# 温度传感器
entity: sensor.esp32c3_3340_temperature

# 湿度传感器
entity: sensor.esp32c3_3340_humidity

# LED状态
entity: sensor.zusheng_s_esp32c3_ledzhuang_tai

# 重启按钮
entity: button.zusheng_s_esp32c3_zhong_qi_she_bei
```

**优化建议**：

为避免拼音转换和不一致问题，建议修改 `config.py` 中的设备名称和实体名称为纯英文：

```python
"ha": {
    "device_name": "ESP32C3_3340",  # 使用设备ID作为设备名称
    "temp_name": "Temperature",
    "humi_name": "Humidity", 
    "fan_name": "Fan",
}
```

## 更新日志

### 2025-09-08

- 修复风扇转速传感器在 HA 中不显示的问题
- 完善风扇数据发布逻辑，确保读取失败时显示 "N/A"
- 添加风扇转速控制功能（支持 0%、30%、60%、90% 档位）
- 增强 HA 日志和诊断功能
- 为重启按钮添加确认弹窗，防止误操作
- 创建 QA.md 文档，记录常见问题和解决方案
- 修复风扇硬件控制问题：移除硬件定时器，简化初始化逻辑
- 修复 MQTT 消息解析问题：支持字符串格式的 select 命令
- 修正硬件定时器管理器配置：ESP32C3 只有 2 个硬件定时器
- 删除测试代码，保持代码整洁
- 修复风扇转速状态显示问题：保持实际转速值显示
- 设置日志级别为 DEBUG，显示所有错误和警告信息
- 深度修复风扇控制逻辑：移除系统启动强制默认转速，使用纯数字传输
- 优化定期维护逻辑：仅在状态变化时发布，避免覆盖用户设置
- 修正风扇最低转速问题：将 0%改为 10%，恢复系统启动默认转速设置
- 添加风扇起转辅助功能：解决低转速时风扇无法正常启动的问题
- 修复起转辅助逻辑：避免系统启动时 30%转速触发起转辅助，添加 MQTT 调试日志
- 修改风扇转速控制为纯 Gauge 卡片：使用 Home Assistant 原生 Gauge 卡片实现圆形进度环显示
- 提供自动化配置方案：通过脚本自动生成 Lovelace 配置文件，无需手动配置
