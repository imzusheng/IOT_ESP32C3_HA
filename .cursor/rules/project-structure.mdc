---
alwaysApply: true
description: ESP32C3 IoT项目结构和架构指南
---

# ESP32C3 IoT项目结构指南

这是一个基于ESP32-C3的MicroPython物联网设备项目，专为Home Assistant智能家居系统设计。

## 核心架构特点

- **事件驱动架构**: 基于EventBus的松耦合设计，支持模块间异步通信
- **函数式状态机**: 使用函数和字典替代类继承，简化架构提高稳定性
- **内存优化**: 针对ESP32C3的264KB内存限制，采用对象池、静态缓存等技术
- **软件定时系统**: 基于diff时间的主循环，节省硬件定时器资源

## 项目结构

```
app/                        # 开发源代码目录(编译后上传到设备根目录)
├── lib/                    # 核心库模块
│   ├── event_bus_lock.py  # 事件总线(含事件常量)
│   ├── logger.py          # 极简日志系统
│   ├── async_runtime.py   # 异步运行时
│   └── umqtt_lock.py      # MQTT客户端库
├── hw/                     # 硬件抽象层
│   ├── led.py            # LED控制器(开箱即用)
│   └── sht40.py          # SHT40温湿度传感器
├── net/                    # 网络通信层
│   ├── network_manager.py # 网络管理器
│   ├── wifi.py           # WiFi管理器
│   ├── mqtt.py           # MQTT控制器
│   └── ntp.py            # NTP时间同步
├── utils/                  # 工具函数
│   ├── json_utils.py     # JSON工具函数
│   ├── time_utils.py     # 时间工具函数
│   └── timers.py         # 定时器工具集
├── main.py                # 主程序入口
├── state_machine.py       # 状态机实现
├── config.py              # 配置管理
├── daemon.py              # 守护服务
├── ha.py                  # Home Assistant集成
└── boot.py                # 启动引导
```

## 核心文件说明

- [main.py](mdc:app/main.py): 主程序入口，负责系统初始化和主循环
- [config.py](mdc:app/config.py): 集中式配置管理，包含所有系统配置
- [state_machine.py](mdc:app/state_machine.py): 函数式状态机，管理设备状态转换
- [daemon.py](mdc:app/daemon.py): 守护服务，监控系统健康状态
- [ha.py](mdc:app/ha.py): Home Assistant集成，处理设备发现和状态上报

## 硬件限制

- **内存**: ESP32C3只有264KB SRAM，必须时刻注意内存使用
- **定时器**: 最多支持2个硬件定时器(编号0至1)
- **Flash**: 4MB Flash存储空间
- **CPU**: 160MHz主频

## 开发原则

1. **事件驱动**: 所有模块间通信通过EventBus进行
2. **内存优化**: 注意ESP32C3的264KB内存限制
3. **错误处理**: 统一的错误处理和恢复机制
4. **状态管理**: 使用函数式状态机管理设备状态
5. **配置中心**: 所有配置在config.py中统一管理
6. **日志记录**: 统一的日志系统和错误追踪
7. **资源优化**: 使用软件定时替代硬件定时器，节省系统资源